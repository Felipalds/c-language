// criar uma struct para representar um ponto
// ler tres pontos
// calcular tres distancias
// a distancia entra dois pontos tem que ser menor que a soma das outras distancias

#include <stdio.h>

typedef struct{
    float x;
    float y;
    float z;
} TPontos;

int main(){

    float dist1, dist2, dist3;
    TPontos A, B, C;
    while(scanf("%f %f %f %f %f %f %f %f %f", &A.x, &A.y, &A.z, &B.x, &B.y, &B.z, &C.x, &C.y, &C.z) != EOF){
        
    }

    return 0;
}